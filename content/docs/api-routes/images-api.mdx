---
title: Images API
description: Fetches your images directly from the cloudinary database.
---



# Installation

### CLI

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/images-api.json
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm dlx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/images-api.json
    ```
    </Tab>

    <Tab>
    ```bash
    yarn dlx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/images-api.json
    ```
    </Tab>

    <Tab>
    ```bash
    bunx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/images-api.json
    ```
    </Tab>
</Tabs>

### Manual

<Steps>

<Step>
1. Install the required dependencies:

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npm install cloudinary
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm add cloudinary
    ```
    </Tab>

    <Tab>
    ```bash
    yarn add cloudinary
    ```
    </Tab>

    <Tab>
    ```bash
    bun add cloudinary
    ```
    </Tab>
</Tabs>
</Step>

<Step>

2. Add the route.ts into *app/api/images/* folder.

<ComponentSource name="images-api" showAllFiles={true} />
</Step>


</Steps>