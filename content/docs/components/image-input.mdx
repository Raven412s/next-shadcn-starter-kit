---
title: Image Input 
description: Uploads your images directly to the cloudinary database.
---

# Preview

<Tabs items={["Preview", "Code"]} >
    <Tab value="Preview">
        <ComponentPreview Component={<ImageInput/>} />
    </Tab>

    <Tab value="Code" 
    data-leniss-prevent
    >
     <ComponentSource name="image-input" showAllFiles={true} />
    </Tab>
</Tabs>

## Installation

### CLI

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/image-input.json
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm dlx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/image-input.json
    ```
    </Tab>

    <Tab>
    ```bash
    yarn dlx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/image-input.json
    ```
    </Tab>

    <Tab>
    ```bash
    bunx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/image-input.json
    ```
    </Tab>
</Tabs>

### Manual

<Steps>

<Step>
1. Install the required dependencies:

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npm install cloudinary sonner
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm add cloudinary sonner
    ```
    </Tab>

    <Tab>
    ```bash
    yarn add cloudinary sonner
    ```
    </Tab>

    <Tab>
    ```bash
    bun add cloudinary sonner
    ```
    </Tab>
</Tabs>
</Step>

<Step>

2. Add the Input and Button components to your project.

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npx shadcn@latest add input button
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm dlx shadcn@latest add input button
    ```
    </Tab>

    <Tab>
    ```bash
    yarn dlx shadcn@latest add input button
    ```
    </Tab>

    <Tab>
    ```bash
    bunx shadcn@latest add input button
    ```
    </Tab>
</Tabs>
</Step>
</Steps>

### and you are done!