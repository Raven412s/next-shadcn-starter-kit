---
title: Image Upload Multiple 
description: Uploads multiple images in a queue directly to your cloudinary database. Come with a progress bar and Dropzone.
---

# Preview

<Tabs items={["Preview", "Code"]} >
    <Tab value="Preview">
    <ComponentPreview Component={<ImageUploadMultiple />} />
    </Tab>

    <Tab value="Code" 
    data-leniss-prevent
    >
     <ComponentSource name="image-upload-multiple" showAllFiles={true} />
    </Tab>
</Tabs>

## Installation

### CLI

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/image-upload-multiple.json
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm dlx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/image-upload-multiple.json
    ```
    </Tab>

    <Tab>
    ```bash
    yarn dlx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/image-upload-multiple.json
    ```
    </Tab>

    <Tab>
    ```bash
    bunx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/image-upload-multiple.json
    ```
    </Tab>
</Tabs>

### Manual

<Steps>

<Step>
1. Install the required dependencies:

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npm install cloudinary sonner
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm add cloudinary sonner
    ```
    </Tab>

    <Tab>
    ```bash
    yarn add cloudinary sonner
    ```
    </Tab>

    <Tab>
    ```bash
    bun add cloudinary sonner
    ```
    </Tab>
</Tabs>
</Step>

<Step>

2. Add the Input and Button components to your project.

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npx shadcn@latest add input button image-input
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm dlx shadcn@latest add input button image-input
    ```
    </Tab>

    <Tab>
    ```bash
    yarn dlx shadcn@latest add input button image-input
    ```
    </Tab>

    <Tab>
    ```bash
    bunx shadcn@latest add input button image-input
    ```
    </Tab>
</Tabs>
</Step>
</Steps>

### and you are done!