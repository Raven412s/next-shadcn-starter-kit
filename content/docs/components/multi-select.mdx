---
title: "Multi Select"
description: "A customizable multi-select dropdown component with badges and search functionality."
slug: "/docs/components/multi-select"
tags: "components, multi-select, dropdown, select, form, UI"
---

# Preview

<Tabs items={["Preview", "Code"]}>
    <Tab value="Preview">
        <ComponentPreview Component={<MultiSelect/>} />
    </Tab>

    <Tab value="Code" data-leniss-prevent>
        <ComponentSource name="multi-select" />
    </Tab>
</Tabs>

## Installation

### CLI

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/multi-select.json
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm dlx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/multi-select.json
    ```
    </Tab>

    <Tab>
    ```bash
    yarn dlx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/multi-select.json
    ```
    </Tab>

    <Tab>
    ```bash
    bunx shadcn@latest add https://starter-kit-nu-two.vercel.app/r/multi-select.json
    ```
    </Tab>
</Tabs>

### Manual

<Steps>

<Step>
1. Install the required dependencies:

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npm install lucide-react
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm add lucide-react
    ```
    </Tab>

    <Tab>
    ```bash
    yarn add lucide-react
    ```
    </Tab>

    <Tab>
    ```bash
    bun add lucide-react
    ```
    </Tab>
</Tabs>
</Step>

<Step>
2. Add the required shadcn components to your project:

<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
    <Tab>
    ```bash
    npx shadcn@latest add button input dropdown-menu badge
    ```
    </Tab>

    <Tab>
    ```bash
    pnpm dlx shadcn@latest add button input dropdown-menu badge
    ```
    </Tab>

    <Tab>
    ```bash
    yarn dlx shadcn@latest add button input dropdown-menu badge
    ```
    </Tab>

    <Tab>
    ```bash
    bunx shadcn@latest add button input dropdown-menu badge
    ```
    </Tab>
</Tabs>
</Step>

<Step>
3. Copy the multi-select component to your project:
<ComponentSource name="multi-select" copy />
</Step>

<Step>
4. And you're done! You can now use the MultiSelect component in your project.
</Step>

</Steps>